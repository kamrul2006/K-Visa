import React from 'react';

const SomeVisa = () => {
    return (
        <div>
            {/* .slice(Math.max(AllVisa.length - 2, 0)) */}

            ho
        </div>
    );
};

export default SomeVisa;